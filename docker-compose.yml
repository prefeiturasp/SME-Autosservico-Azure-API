version: "3.9"

services:
  api:
    build: .
    image: sme-autosservico-apiazure
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    env_file:
      - .env
    environment:
      API_HOST: 0.0.0.0
    ports:
      - "${API_PORT:-8000}:8000"
    restart: unless-stopped
