version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: azure-backlog-api:prod
    env_file:
      - .env
    environment:
      API_HOST: 0.0.0.0
    ports:
      - "${API_PORT:-8000}:8000"
    restart: unless-stopped
